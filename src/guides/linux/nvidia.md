# Nvidia

Applied to Ubuntu 26.04 LTS.

- Run `ubuntu-drivers devices` and check which driver version is marked with `recommended`.
- Use `apt` to install the recommended driver.
- Restart the machine in order to be able to run `nvidia-smi`.
